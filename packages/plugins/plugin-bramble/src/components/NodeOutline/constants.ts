//
// Copyright 2026 DXOS.org
//

/** Per-level indentation of an outline row, in px. Shared by the row's visual indent
 *  (`OutlineRow`) and the drag hitbox's `indentPerLevel` (`useRowDnd`) — they MUST match,
 *  so they read the one value here. */
export const INDENT_PX = 32;
